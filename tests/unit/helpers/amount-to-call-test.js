import { amountToCall } from 'poker-webapp-emberjs/helpers/amount-to-call';
import { module, test } from 'qunit';

module('Unit | Helper | amount to call');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = amountToCall([42]);
  assert.ok(result);
});
